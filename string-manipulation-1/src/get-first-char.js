/* exported getFirstChar */
// Create new function getFirstChar with one parameter, string
// Call method charAt index 0 of string object and assign result to new var firstChar
// return firstChar

function getFirstChar(string) {
  var firstChar = string.charAt(0);
  return firstChar;
}
