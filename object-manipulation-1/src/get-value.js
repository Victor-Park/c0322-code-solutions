/* exported getValue */
// Create new function getValue with two parameters, object and key
// Assign value of object at key property to new var $new
// return $new

function getValue(object, key) {
  var $new = object[key];
  return $new;
}
