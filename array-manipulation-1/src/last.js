/* exported last */
// Create new function last with one parameter, array
// Assign array at last index (array.length-1) to new var $last
// Return $last

function last(array) {
  var $last = array[array.length - 1];
  return $last;
}
