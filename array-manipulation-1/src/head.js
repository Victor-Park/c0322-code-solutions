/* exported head */
// Create new function head with one parameter, array
// Assign array at index 0 to new var $head
// Return $head

function head(array) {
  var $head = array[0];
  return $head;
}
